name: demo-action

on:
  push:
    branches:
      - main
      - master

jobs:
  Job 1 directory actions:
    runs-on: ubuntu-latest
    steps:
      - name: Check Out Code
        uses: actions/checkout@v2
      - name: Show dir
        run: |
            pwd
      - name: List Dir
        run: |
           ls -al

  Job 2 show SHA:
    runs-on: ubuntu-latest
    steps:
      - name: Check Out Code
        uses: actions/checkout@v2
      - name: Show SHA
        run: |
           echo $${{ github.sha }}
